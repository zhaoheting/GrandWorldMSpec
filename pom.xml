<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>2.2.4.RELEASE</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>com.example</groupId>
    <artifactId>GrandWorldMSpec</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>GrandWorldMSpec</name>
    <description>Demo project for Spring Boot</description>

    <properties>
        <java.version>1.8</java.version>
        <swagger-codegen-maven-plugin-version>2.3.1</swagger-codegen-maven-plugin-version>
        <base-package>com.example.GrandWorldMSpec</base-package>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.junit.vintage</groupId>
                    <artifactId>junit-vintage-engine</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!--swagger-->
        <dependency>
            <groupId>io.springfox</groupId>
            <artifactId>springfox-swagger2</artifactId>
            <version>2.8.0</version>
        </dependency>
        <dependency>
            <groupId>io.springfox</groupId>
            <artifactId>springfox-swagger-ui</artifactId>
            <version>2.8.0</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

            <!--code gen-->
            <plugin>
                <!-- swagger codegen 用来生成服务端、客户端、Model等代码 -->
                <groupId>io.swagger</groupId>
                <artifactId>swagger-codegen-maven-plugin</artifactId>
                <version>${swagger-codegen-maven-plugin-version}</version>
                <executions>
                    <execution>
                        <!--execution的标识id-->
                        <id>generate-sources-server</id>
                        <!-- 配置的执行目标 -->
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <!--描述文件的存放路径-->
                            <inputSpec>${project.basedir}/swagger.yaml</inputSpec>
                            <language>spring</language>
                            <!--自动生成的代码的路径由以下三个property共同决定，格式如下"<output>/<sourceFolder>/"+详细路径（即invokerPackage或apiPackage或modelPackage）-->
                            <output>${project.basedir}</output>
                            <!--RFC3339DateFormat.java与Swagger2SpringBoot.java的生成路径，在使用codegen时不必修改这两个类，可以在文件.swagger-codegen-ignore中设置不生成，则此路径无用-->
                            <invokerPackage>${base-package}.generated.controller</invokerPackage>
                            <!--指定swagger配置类中的basepackage中扫描的路径与api接口的生成后存放路径-->
                            <apiPackage>${base-package}.generated.controller.interfaces</apiPackage>
                            <modelPackage>${base-package}.generated.model</modelPackage>
                            <configOptions>
                                <sourceFolder>src/main/java</sourceFolder>
                                <dateLibrary>java8-localdatetime</dateLibrary>
                                <!--<hideGenerationTimestamp>true</hideGenerationTimestamp>-->
                                <useTags>true</useTags><!--是否使用tags命名生成的接口名-->
                                <configPackage>${base-package}.swagger</configPackage><!--配置文件生成路径-->
                            </configOptions>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
